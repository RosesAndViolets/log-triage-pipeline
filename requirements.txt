# Python >= 3.10 (uses `X | None` and builtin generics). Verified on 3.13.2.
google-genai>=2.17.0
pydantic>=2.13.0

# Must stay <2. google-genai 2.17 reads tool.inputSchema; mcp 2.0 renamed it to
# input_schema, so the SDK's own MCP adapter raises AttributeError against 2.x.
mcp>=1.2,<2
