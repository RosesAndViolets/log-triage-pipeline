# Cloned fault-injection target — see README "Setup". Not ours to version.
targets/

__pycache__/
*.pyc
.vscode/
.DS_Store

# State databases and exported replays — build output, not source.
data/

# Exported replays are build output, not source.
map.html
